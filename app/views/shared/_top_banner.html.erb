<div class="AggieScheduleBar">
	<p>AGGIE SENATE</p>
</div>

<div class="navigationBar">
	<%= link_to "Home", @user.admin? ? "/dashboard/admin" : "/dashboard/user" %>
	<%= link_to "Users", "/login" %>
	<%= link_to "General Committee", "/login" %>
	<% if logged_in? %>
    <%= link_to "Logout", "/logout", method: :post %>
  <% else %>
    <%= link_to "Login", "/login" %>
  <% end %>
	<div class="dropdown">
		<button class="dropbtn">Committees</button>
		<div class="dropdown-content">
			<% Committee.all.each do |cInst| %>
				<%# if possbile replace nil with cInst.name %>
				<%= link_to(nil, committee_path(cInst), :class => 'action show') %>
			<% end %>
		</div>
	</div>
</div>
<div class="dropdown">
	<button class="dropbtn">Committees</button>
	<div class="dropdown-content">
		<% Committee.all.each do |cInst| %>
			<%# if possbile replace nil with cInst.name %>
			<%= link_to(nil, committee_path(cInst), :class => 'action show') %>
		<% end %>
	</div>
</div>
